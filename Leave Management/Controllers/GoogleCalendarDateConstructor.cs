using System;

namespace Leave_Management.Controllers
{
    public class GoogleCalendarDateConstructor
    {
        public string dateTime { get; set; }
        public string timeZone { get; set; }

        public GoogleCalendarDateConstructor(String dateTime, String timeZone)
        {
            dateTime = dateTime;
            timeZone = timeZone;
        }

        public dateiniializer(DateTime)
        {
            
            return 
        }
    }
}